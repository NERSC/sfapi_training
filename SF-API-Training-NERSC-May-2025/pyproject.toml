[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "sfapi-training-2025"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["jupyter", "matplotlib", "pandas", "sfapi-client>=0.4.1"]
